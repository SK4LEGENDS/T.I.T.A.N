{
  "name": "smart-timetable",
  "version": "1.0.0",
  "description": "Smart Timetable Generator with Agentic AI",
  "scripts": {
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "start:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend && call .\\venv\\Scripts\\activate.bat && uvicorn main:app --reload --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
